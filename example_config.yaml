author: "Author Name"
output_file: "dataset.jsonl"
chunk_size: 1024  # number of characters per chunk
overlap: 0.25
report_file: "split_report.json"
books:
  - path: "path/to/book1.pdf"
    start_page: 1
    end_page: 100
  - path: "path/to/book2.epub"
    start_page: 5
    end_page: 50
  # Directory containing additional books
  - path: "path/to/book_directory"  # loads all PDFs and EPUBs inside
